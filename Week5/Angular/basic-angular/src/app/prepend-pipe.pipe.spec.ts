import { PrependPipePipe } from './prepend-pipe.pipe';

describe('PrependPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PrependPipePipe();
    expect(pipe).toBeTruthy();
  });
});
